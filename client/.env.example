# Backend API URL (used in production when frontend is on Vercel, backend on Render)
# In local dev, Vite proxies /api to localhost:3001 so this isn't needed
VITE_API_URL=https://your-backend-url.onrender.com
